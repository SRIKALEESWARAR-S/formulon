# Formulon Physics Formula Catalog

**Public formulas: 234**

| Module | Formula | Validation | Real-world use case |
|---|---|---|---|
| `classicalmechanics` | `average_velocity(delta_x: float, delta_t: float)` | `delta_x=RULES.DISPLACEMENT, delta_t=RULES.POSITIVE_TIME` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate the average velocity over a given spatial displacement and time interval ($v_{avg} = \\Delta x / \\Delta t$). Project: Project Formulon-Physics |
| `classicalmechanics` | `average_acceleration(delta_v: float, delta_t: float)` | `delta_v=RULES.VELOCITY, delta_t=RULES.POSITIVE_TIME` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate the average linear acceleration ($a_{avg} = \\Delta v / \\Delta t$). Project: Project Formulon-Physics |
| `classicalmechanics` | `displacement_constant_a(v0: float, t: float, a: float)` | `v0=RULES.VELOCITY, t=RULES.NON_NEG_TIME, a=RULES.ACCELERATION` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate displacement under constant acceleration ($x = v_0 t + \\frac{1}{2}a t^2$). Project: Project Formulon-Physics |
| `classicalmechanics` | `final_velocity_constant_a(v0: float, a: float, t: float)` | `v0=RULES.VELOCITY, a=RULES.ACCELERATION, t=RULES.NON_NEG_TIME` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate final velocity under constant acceleration ($v = v_0 + a t$). Project: Project Formulon-Physics |
| `classicalmechanics` | `velocity_displacement_relation(v0: float, a: float, delta_x: float)` | `v0=RULES.VELOCITY, a=RULES.ACCELERATION, delta_x=RULES.DISPLACEMENT` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate final velocity magnitude squared relation ($v = \\sqrt{v_0^2 + 2 a \\Delta x}$). Project: Project Formulon-Physics |
| `classicalmechanics` | `displacement_from_velocities(v0: float, v: float, t: float)` | `v0=RULES.VELOCITY, v=RULES.VELOCITY, t=RULES.NON_NEG_TIME` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate displacement using initial and final velocities under uniform acceleration ($x = \\frac{v_0 + v}{2} t$). Project: Project Formulon-Physics |
| `classicalmechanics` | `freefall_velocity(g: float, t: float)` | `g=RULES.GRAVITY, t=RULES.NON_NEG_TIME` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate the speed of an object dropped from rest under standard freefall conditions ($v = g t$). Project: Project Formulon-Physics |
| `classicalmechanics` | `freefall_height(g: float, t: float)` | `g=RULES.GRAVITY, t=RULES.NON_NEG_TIME` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate the vertical drop distance of an object from rest under freefall ($h = \\frac{1}{2}g t^2$). Project: Project Formulon-Physics |
| `classicalmechanics` | `time_of_flight_vertical(h: float, g: float)` | `h=RULES.HEIGHT, g=RULES.GRAVITY` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate time to reach baseline floor when dropped vertically from height h ($t = \\sqrt{2h/g}$). Project: Project Formulon-Physics |
| `classicalmechanics` | `projectile_time_of_flight(v0: float, theta_deg: float, g: float)` | `v0=RULES.SPEED, theta_deg=RULES.ANGLE_DEG_90, g=RULES.GRAVITY` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate total projectile time of flight over symmetric plane surfaces ($T = \\frac{2 v_0 \\sin\\theta}{g}$). Project: Project Formulon-Physics |
| `classicalmechanics` | `projectile_max_height(v0: float, theta_deg: float, g: float)` | `v0=RULES.SPEED, theta_deg=RULES.ANGLE_DEG_90, g=RULES.GRAVITY` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate maximum trajectory vertex elevation height reached by a projectile ($H = \\frac{v_0^2 \\sin^2\\theta}{2g}$). Project: Project Formulon-Physics |
| `classicalmechanics` | `projectile_range(v0: float, theta_deg: float, g: float)` | `v0=RULES.SPEED, theta_deg=RULES.ANGLE_DEG_90, g=RULES.GRAVITY` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate horizontal ground downrange footprint displacement achieved ($R = \\frac{v_0^2 \\sin(2\\theta)}{g}$). Project: Project Formulon-Physics |
| `classicalmechanics` | `projectile_trajectory_y(x: float, v0: float, theta_deg: float, g: float)` | `x=RULES.DISTANCE, v0=RULES.SPEED, theta_deg=RULES.ANGLE_DEG_90, g=RULES.GRAVITY` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Solve for trajectory altitude coordinate y given horizontal downrange index position x via the trajectory equation. Project: Project Formulon-Physics |
| `classicalmechanics` | `newtons_second_law(mass: float, acceleration: float)` | `mass=RULES.MASS, acceleration=RULES.ACCELERATION` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate net active force acting on an inertial system ($F = m a$). Project: Project Formulon-Physics |
| `classicalmechanics` | `weight(mass: float, g: float)` | `mass=RULES.MASS, g=RULES.GRAVITY` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate localized static weight force vector magnitude ($W = m g$). Project: Project Formulon-Physics |
| `classicalmechanics` | `linear_momentum(mass: float, velocity: float)` | `mass=RULES.MASS, velocity=RULES.VELOCITY` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate linear momentum of a translation system coordinate ($p = m v$). Project: Project Formulon-Physics |
| `classicalmechanics` | `newtons_second_law_derivative(delta_p: float, delta_t: float)` | `delta_p=RULES.MOMENTUM, delta_t=RULES.POSITIVE_TIME` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate force acting as a direct time rate change of linear momentum system state ($F = \\Delta p / \\Delta t$). Project: Project Formulon-Physics |
| `classicalmechanics` | `impulse_constant_force(force: float, delta_t: float)` | `force=RULES.FORCE, delta_t=RULES.POSITIVE_TIME` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate structural linear impulse under a constant force payload window ($J = F \\Delta t$). Project: Project Formulon-Physics |
| `classicalmechanics` | `impulse_variable_force(force_func: Callable[[float], float], t_start: float, t_end: float)` | `mathematical checks in implementation` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate impulse delivered across time tracking profiles under fluctuating variable load windows ($J = \\int F(t) dt$). Project: Project Formulon-Physics |
| `classicalmechanics` | `static_friction_max(mu_s: float, normal_force: float)` | `mu_s=RULES.COEFF_FRICTION, normal_force=RULES.NORMAL_FORCE` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate absolute limit threshold value before structural boundary interface slippage occurs ($f_{s,max} = \\mu_s F_n$). Project: Project Formulon-Physics |
| `classicalmechanics` | `kinetic_friction(mu_k: float, normal_force: float)` | `mu_k=RULES.COEFF_FRICTION, normal_force=RULES.NORMAL_FORCE` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate uniform interface dynamic friction mechanical constraint value during slippage ($f_k = \\mu_k F_n$). Project: Project Formulon-Physics |
| `classicalmechanics` | `hookes_law(k: float, x: float)` | `k=RULES.SPRING_CONST, x=RULES.SPRING_DISPLACEMENT` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate structural physical linear restoring force vector magnitude of an elastic spring model ($F = -k x$). Project: Project Formulon-Physics |
| `classicalmechanics` | `apparent_weight_upward(mass: float, g: float, a_up: float)` | `mass=RULES.MASS, g=RULES.GRAVITY, a_up=RULES.NON_NEGATIVE` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate internal apparent weight inside an upward accelerated frame system container ($N = m(g + a)$). Project: Project Formulon-Physics |
| `classicalmechanics` | `apparent_weight_downward(mass: float, g: float, a_down: float)` | `mass=RULES.MASS, g=RULES.GRAVITY, a_down=RULES.NON_NEGATIVE` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate internal apparent weight inside a downward accelerated frame environment container ($N = m(g - a)$). Project: Project Formulon-Physics |
| `classicalmechanics` | `angular_velocity(delta_theta: float, delta_t: float)` | `delta_theta=RULES.REAL, delta_t=RULES.POSITIVE_TIME` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate average scalar angular velocity metric ($\omega = \\Delta \\theta / \\Delta t$). Project: Project Formulon-Physics |
| `classicalmechanics` | `angular_acceleration(delta_omega: float, delta_t: float)` | `delta_omega=RULES.REAL, delta_t=RULES.POSITIVE_TIME` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate average scalar angular acceleration tracking indicator ($\\alpha = \\Delta \omega / \\Delta t$). Project: Project Formulon-Physics |
| `classicalmechanics` | `linear_displacement_from_angular(radius: float, theta_rad: float)` | `radius=RULES.RADIUS, theta_rad=RULES.REAL` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Translate pure localized angular metric tracking dimensions directly into linear arc lengths ($s = r \\theta$). Project: Project Formulon-Physics |
| `classicalmechanics` | `linear_velocity_from_angular(radius: float, omega_rad_s: float)` | `radius=RULES.RADIUS, omega_rad_s=RULES.REAL` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Determine tangential edge translational velocity component vector magnitude scales ($v = r \omega$). Project: Project Formulon-Physics |
| `classicalmechanics` | `tangential_acceleration(radius: float, alpha_rad_s2: float)` | `radius=RULES.RADIUS, alpha_rad_s2=RULES.REAL` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Evaluate translation edge boundary tangential structural acceleration properties ($a_t = r \\alpha$). Project: Project Formulon-Physics |
| `classicalmechanics` | `centripetal_acceleration(v: float, radius: float)` | `v=RULES.SPEED, radius=RULES.RADIUS` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate inbound center directed uniform centripetal coordinate acceleration components ($a_c = v^2 / r$). Project: Project Formulon-Physics |
| `classicalmechanics` | `centripetal_force(mass: float, v: float, radius: float)` | `mass=RULES.MASS, v=RULES.SPEED, radius=RULES.RADIUS` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate active vector constraint forces required to sustain stable curvature trajectory loops ($F_c = m v^2 / r$). Project: Project Formulon-Physics |
| `classicalmechanics` | `rotational_final_velocity(omega0: float, alpha: float, t: float)` | `omega0=RULES.REAL, alpha=RULES.REAL, t=RULES.NON_NEG_TIME` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate final state angular velocity under uniform coordinate angular acceleration ($\omega = \omega_0 + \\alpha t$). Project: Project Formulon-Physics |
| `classicalmechanics` | `rotational_displacement(omega0: float, t: float, alpha: float)` | `omega0=RULES.REAL, t=RULES.NON_NEG_TIME, alpha=RULES.REAL` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate angular rotation displacement tracking index coordinates ($\\theta = \omega_0 t + \\frac{1}{2}\\alpha t^2$). Project: Project Formulon-Physics |
| `classicalmechanics` | `rotational_velocity_displacement_relation(omega0: float, alpha: float, delta_theta: float)` | `omega0=RULES.REAL, alpha=RULES.REAL, delta_theta=RULES.REAL` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate rotational angular velocity relationship thresholds ($\omega = \\sqrt{\omega_0^2 + 2 \\alpha \\Delta \\theta}$). Project: Project Formulon-Physics |
| `classicalmechanics` | `torque(radius: float, force: float, theta_deg: float)` | `radius=RULES.RADIUS, force=RULES.FORCE, theta_deg=RULES.ANGLE_DEG` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate structural directional lever arm twisting torque force moments ($\\tau = r F \\sin\\theta$). Project: Project Formulon-Physics |
| `classicalmechanics` | `discrete_moment_of_inertia(masses: list[float], radii: list[float])` | `mathematical checks in implementation` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate total rotational moment of inertia across collections of point mass distribution nodes ($I = \\sum m_i r_i^2$). Project: Project Formulon-Physics |
| `classicalmechanics` | `continuous_moment_of_inertia(density_func: Callable[[float], float], r_min: float, r_max: float)` | `mathematical checks in implementation` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Integrate rotational system profiles to determine continuous volume mass inertia metrics ($I = \\int r^2 dm$). Project: Project Formulon-Physics |
| `classicalmechanics` | `parallel_axis_theorem(i_cm: float, mass: float, d: float)` | `i_cm=RULES.MOMENT_OF_INERTIA, mass=RULES.MASS, d=RULES.PARALLEL_AXIS_D` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Shift tracking center axes references utilizing the parallel axis translation equation ($I = I_{cm} + m d^2$). Project: Project Formulon-Physics |
| `classicalmechanics` | `perpendicular_axis_theorem(i_x: float, i_y: float)` | `i_x=RULES.MOMENT_OF_INERTIA, i_y=RULES.MOMENT_OF_INERTIA` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Evaluate structural planar reference sheet rotational inertia layers via perpendicular coordinate axes ($I_z = I_x + I_y$). Project: Project Formulon-Physics |
| `classicalmechanics` | `angular_momentum_particle(radius: float, linear_momentum_val: float, theta_deg: float)` | `radius=RULES.RADIUS, linear_momentum_val=RULES.REAL, theta_deg=RULES.ANGLE_DEG` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Determine specific localized vector orbital angular momentum values tracking dynamic particles ($L = r p \\sin\\theta$). Project: Project Formulon-Physics |
| `classicalmechanics` | `angular_momentum_rigid_body(i: float, omega_rad_s: float)` | `i=RULES.MOMENT_OF_INERTIA, omega_rad_s=RULES.REAL` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Determine consolidated macroscopic angular momentum vectors across rigid bodies ($L = I \omega$). Project: Project Formulon-Physics |
| `classicalmechanics` | `torque_angular_momentum_relation(delta_l: float, delta_t: float)` | `delta_l=RULES.REAL, delta_t=RULES.POSITIVE_TIME` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate dynamic angular torque as direct continuous time rates of angular momentum mutations ($\\tau = \\Delta L / \\Delta t$). Project: Project Formulon-Physics |
| `classicalmechanics` | `work_constant_force(force: float, displacement: float, theta_deg: float)` | `force=RULES.FORCE, displacement=RULES.DISPLACEMENT, theta_deg=RULES.WORK_ANGLE_DEG` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate mechanical work translations completed by absolute uniform constant directional loads ($W = F d \\cos\\theta$). Project: Project Formulon-Physics |
| `classicalmechanics` | `work_variable_force(force_func: Callable[[float], float], x_start: float, x_end: float)` | `mathematical checks in implementation` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Evaluate energetic integrals determining work distributions matching continuous changing forces ($W = \\int F(x) dx$). Project: Project Formulon-Physics |
| `classicalmechanics` | `translational_kinetic_energy(mass: float, velocity: float)` | `mass=RULES.MASS, velocity=RULES.VELOCITY` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate active macroscopic translational kinetic state work limits ($K = \\frac{1}{2}m v^2$). Project: Project Formulon-Physics |
| `classicalmechanics` | `rotational_kinetic_energy(i: float, omega_rad_s: float)` | `i=RULES.MOMENT_OF_INERTIA, omega_rad_s=RULES.REAL` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate internal spin-state energy metrics across rotating bodies ($K_{rot} = \\frac{1}{2}I \omega^2$). Project: Project Formulon-Physics |
| `classicalmechanics` | `gravitational_potential_energy(mass: float, g: float, height: float)` | `mass=RULES.MASS, g=RULES.GRAVITY, height=RULES.REAL` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Determine localized configuration potential energy balances within regular constant fields ($U_g = m g h$). Project: Project Formulon-Physics |
| `classicalmechanics` | `elastic_potential_energy(k: float, delta_x: float)` | `k=RULES.SPRING_CONST, delta_x=RULES.SPRING_DISPLACEMENT` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Evaluate structural internal conservation grid power loaded into compressed springs ($U_s = \\frac{1}{2}k x^2$). Project: Project Formulon-Physics |
| `classicalmechanics` | `work_energy_theorem_translational(w_net: float)` | `w_net=RULES.REAL` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Express net external spatial load allocations as direct mutations across total kinetic fields ($\\Delta K = W_{net}$). Project: Project Formulon-Physics |
| `classicalmechanics` | `average_power(work: float, delta_t: float)` | `work=RULES.REAL, delta_t=RULES.POSITIVE_TIME` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Measure gross average energetic dissipation throughput velocities ($P_{avg} = W / \\Delta t$). Project: Project Formulon-Physics |
| `classicalmechanics` | `instantaneous_power(force: float, velocity: float)` | `force=RULES.FORCE, velocity=RULES.SPEED` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Map point-in-time work injection velocities based on load configurations ($P = F v$). Project: Project Formulon-Physics |
| `classicalmechanics` | `mechanical_efficiency(work_out: float, work_in: float)` | `work_out=RULES.WORK_IO, work_in=RULES.WORK_IO` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Measure systemic mechanical efficiency percentages comparing load footprints ($\\eta = [W_{out} / W_{in}] \\times 100$). Project: Project Formulon-Physics |
| `classicalmechanics` | `center_mass_discrete(masses: list[float], coordinates: list[float])` | `mathematical checks in implementation` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Determine the composite structural mean balance point across discrete distributed particle systems ($x_{cm} = \\frac{\\sum m_i x_i}{\\sum m_i}$). Project: Project Formulon-Physics |
| `classicalmechanics` | `newtons_law_of_gravitation(m1: float, m2: float, r: float)` | `m1=RULES.MASS, m2=RULES.MASS, r=RULES.RADIUS` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate cosmic gravitational attraction forces shared by isolated physical systems ($F = G \\frac{m_1 m_2}{r^2}$). Project: Project Formulon-Physics |
| `classicalmechanics` | `gravitational_field_strength(m_source: float, r: float)` | `m_source=RULES.MASS, r=RULES.RADIUS` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Evaluate structural cosmic pull fields active at external radial separation indexes ($g = G M / r^2$). Project: Project Formulon-Physics |
| `classicalmechanics` | `gravitational_potential(m_source: float, r: float)` | `m_source=RULES.MASS, r=RULES.RADIUS` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Evaluate specific potential metrics representing mechanical position states within deep spaces ($V = -G M / r$). Project: Project Formulon-Physics |
| `classicalmechanics` | `gravitational_potential_energy_cosmic(m1: float, m2: float, r: float)` | `m1=RULES.MASS, m2=RULES.MASS, r=RULES.RADIUS` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Evaluate absolute continuous macroscopic mechanical binding energies spanning orbital scales ($U = -G \\frac{m_1 m_2}{r}$). Project: Project Formulon-Physics |
| `classicalmechanics` | `orbital_velocity(m_central: float, r: float)` | `m_central=RULES.MASS, r=RULES.RADIUS` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate stable horizontal trajectory velocities required to maintain circular paths ($v = \\sqrt{G M / r}$). Project: Project Formulon-Physics |
| `classicalmechanics` | `escape_velocity(m_central: float, r: float)` | `m_central=RULES.MASS, r=RULES.RADIUS` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Calculate entry launch thresholds needed to exit localized gravitational field wells ($v_{esc} = \\sqrt{2 G M / r}$). Project: Project Formulon-Physics |
| `classicalmechanics` | `keplers_third_law_period(m_central: float, semi_major_axis_r: float)` | `m_central=RULES.MASS, semi_major_axis_r=RULES.RADIUS` | Engineering mechanics: vehicles, machines, robotics, projectiles, structures and orbital estimates. Determine structural periodic orbital cycle parameters via Kepler's harmonic matching ($T = \\sqrt{\\frac{4\\pi^2 r^3}{G M}}$). Project: Project Formulon-Physics |
| `electromagnetism` | `coulomb_force(q1: float, q2: float, r: float)` | `q1=RULES.REAL, q2=RULES.REAL, r=RULES.RADIUS` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Calculate electrostatic attraction or repulsion force between localized charges ($F = \\frac{1}{4\\pi\\epsilon_0}\\frac{q_1 q_2}{r^2}$). Project: Project Formulon-Physics |
| `electromagnetism` | `electric_field_strength(force: float, charge: float)` | `force=RULES.FORCE, charge=RULES.CHARGE` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Evaluate structural electric field vectors experienced by localized probe charges ($E = F / q$). Project: Project Formulon-Physics |
| `electromagnetism` | `point_charge_electric_field(q: float, r: float)` | `q=RULES.REAL, r=RULES.RADIUS` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Evaluate point source electric fields matching radial isolation tracking scales ($E = \\frac{1}{4\\pi\\epsilon_0}\\frac{q}{r^2}$). Project: Project Formulon-Physics |
| `electromagnetism` | `electric_flux(field_func: Callable[[float, float], float], area: float, theta_deg: float)` | `area=RULES.AREA, theta_deg=RULES.ANGLE_DEG` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Evaluate static electric surface field flux metrics across simple planar window bounds ($\Phi_E = E A \\cos\\theta$). Project: Project Formulon-Physics |
| `electromagnetism` | `gauss_law_enclosed_charge(q_encl: float)` | `q_encl=RULES.REAL` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Determine net surface field flux matching enclosed boundary charge profiles via Gauss's model ($\Phi_E = Q_{encl}/\\epsilon_0$). Project: Project Formulon-Physics |
| `electromagnetism` | `electric_potential_energy(q1: float, q2: float, r: float)` | `q1=RULES.REAL, q2=RULES.REAL, r=RULES.RADIUS` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Evaluate conservation electrostatic binding potential energy metrics ($U = \\frac{1}{4\\pi\\epsilon_0}\\frac{q_1 q_2}{r}$). Project: Project Formulon-Physics |
| `electromagnetism` | `electric_potential(energy: float, charge: float)` | `energy=RULES.REAL, charge=RULES.CHARGE` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Determine local work potentials matching specific metric positions ($V = U / q$). Project: Project Formulon-Physics |
| `electromagnetism` | `electric_potential_from_field(field_func: Callable[[float], float], l_start: float, l_end: float)` | `mathematical checks in implementation` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Integrate electric vector fields determining potential transitions down line paths ($V = -\\int E \\cdot dl$). Project: Project Formulon-Physics |
| `electromagnetism` | `electric_dipole_moment(charge: float, separation_d: float)` | `charge=RULES.CHARGE, separation_d=RULES.DISTANCE` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Calculate primary electrostatic physical dipole distribution metrics ($p = q d$). Project: Project Formulon-Physics |
| `electromagnetism` | `torque_on_electric_dipole(moment_p: float, field_e: float, theta_deg: float)` | `moment_p=RULES.REAL, field_e=RULES.REAL, theta_deg=RULES.ANGLE_DEG` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Calculate rotational mechanical moments applied across charge pairs inside static tracking fields ($\\tau = p E \\sin\\theta$). Project: Project Formulon-Physics |
| `electromagnetism` | `potential_energy_of_electric_dipole(moment_p: float, field_e: float, theta_deg: float)` | `moment_p=RULES.REAL, field_e=RULES.REAL, theta_deg=RULES.ANGLE_DEG` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Evaluate configuration orientation energetic metrics for electrical dipoles ($U = -p E \\cos\\theta$). Project: Project Formulon-Physics |
| `electromagnetism` | `capacitance(charge: float, voltage: float)` | `charge=RULES.REAL, voltage=RULES.VOLTAGE` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Measure standard operational electrostatic capacity bounds ($C = Q / V$). Project: Project Formulon-Physics |
| `electromagnetism` | `parallel_plate_capacitance(plate_area_a: float, separation_d: float)` | `plate_area_a=RULES.AREA, separation_d=RULES.DISTANCE` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Evaluate structural metrics calculating capacities across geometric sheets ($C = \\epsilon_0 A / d$). Project: Project Formulon-Physics |
| `electromagnetism` | `dielectric_capacitance(dielectric_kappa: float, c_vacuum: float)` | `dielectric_kappa=RULES.DIELECTRIC_CONSTANT, c_vacuum=RULES.REAL` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Scale baseline charge storage capacities using material dielectric properties ($C = \\kappa C_0$). Project: Project Formulon-Physics |
| `electromagnetism` | `capacitors_series_equivalent(capacitances: list[float])` | `mathematical checks in implementation` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Calculate composite series capacity equivalence parameters ($\\frac{1}{C_{eq}} = \\sum \\frac{1}{C_i}$). Project: Project Formulon-Physics |
| `electromagnetism` | `capacitors_parallel_equivalent(capacitances: list[float])` | `mathematical checks in implementation` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Sum capacity configurations matching parallel electrical pathways ($C_{eq} = \\sum C_i$). Project: Project Formulon-Physics |
| `electromagnetism` | `capacitor_stored_energy(c: float, voltage: float)` | `c=RULES.REAL, voltage=RULES.VOLTAGE` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Measure conservation electrostatic work thresholds stacked inside charged fields ($U = \\frac{1}{2}C V^2$). Project: Project Formulon-Physics |
| `electromagnetism` | `electric_field_energy_density(field_e: float)` | `field_e=RULES.REAL` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Evaluate localized specific electric potential energy density metrics ($u_E = \\frac{1}{2}\\epsilon_0 E^2$). Project: Project Formulon-Physics |
| `electromagnetism` | `lorentz_magnetic_force(charge: float, velocity: float, field_b: float, theta_deg: float)` | `charge=RULES.REAL, velocity=RULES.SPEED, field_b=RULES.MAGNETIC_FIELD, theta_deg=RULES.ANGLE_DEG` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Measure structural magnetic deflection vectors handling moving points via Lorentz limits ($F = q v B \\sin\\theta$). Project: Project Formulon-Physics |
| `electromagnetism` | `wire_magnetic_force(current: float, length: float, field_b: float, theta_deg: float)` | `current=RULES.CURRENT, length=RULES.DISTANCE, field_b=RULES.MAGNETIC_FIELD, theta_deg=RULES.ANGLE_DEG` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Calculate composite deflection load distributions sustained over conductive wires ($F = I L B \\sin\\theta$). Project: Project Formulon-Physics |
| `electromagnetism` | `biot_savart_magnitude(current: float, length_dl: float, r: float, theta_deg: float)` | `current=RULES.CURRENT, length_dl=RULES.DISTANCE, r=RULES.RADIUS, theta_deg=RULES.ANGLE_DEG` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Predict differential flux density tracking metrics utilizing the Biot-Savart equation ($dB = \\frac{\\mu_0}{4\\pi}\\frac{I dl \\sin\\theta}{r^2}$). Project: Project Formulon-Physic |
| `electromagnetism` | `amperes_law_enclosed_current(i_encl: float)` | `i_encl=RULES.CURRENT` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Evaluate circulation loop limits given enclosed structural currents via Ampere's law ($\oint B \\cdot dl = \\mu_0 I_{encl}$). Project: Project Formulon-Physics |
| `electromagnetism` | `straight_wire_magnetic_field(current: float, r: float)` | `current=RULES.CURRENT, r=RULES.RADIUS` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Evaluate induced field vectors matching infinite linear current lines ($B = \\frac{\\mu_0 I}{2\\pi r}$). Project: Project Formulon-Physics |
| `electromagnetism` | `solenoid_magnetic_field(turns_per_meter_n: float, current: float)` | `turns_per_meter_n=RULES.TURNS_PER_LENGTH, current=RULES.CURRENT` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Predict internal uniform magnetic field layers inside long structural solenoids ($B = \\mu_0 n I$). Project: Project Formulon-Physics |
| `electromagnetism` | `magnetic_dipole_moment(turns_n: int, current: float, area: float)` | `turns_n=RULES.NON_NEGATIVE, current=RULES.CURRENT, area=RULES.AREA` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Determine tracking loop magnetic vector moments ($\mu = N I A$). Project: Project Formulon-Physics |
| `electromagnetism` | `torque_on_magnetic_dipole(moment_mu: float, field_b: float, theta_deg: float)` | `moment_mu=RULES.REAL, field_b=RULES.MAGNETIC_FIELD, theta_deg=RULES.ANGLE_DEG` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Measure twisting mechanics acting across loop moment distributions inside field parameters ($\\tau = \\mu B \\sin\\theta$). Project: Project Formulon-Physics |
| `electromagnetism` | `electric_current(delta_q: float, delta_t: float)` | `delta_q=RULES.REAL, delta_t=RULES.POSITIVE_TIME` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Calculate standard macroscopic charge transit flow rates ($I = \\Delta q / \\Delta t$). Project: Project Formulon-Physics |
| `electromagnetism` | `current_density_drift(carrier_density_n: float, unit_charge_q: float, drift_velocity_vd: float)` | `carrier_density_n=RULES.NUMBER_DENSITY, unit_charge_q=RULES.REAL, drift_velocity_vd=RULES.SPEED` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Measure localized charge vector flux concentrations mapping particle kinetics ($J = n q v_d$). Project: Project Formulon-Physics |
| `electromagnetism` | `ohms_law_voltage(current: float, resistance: float)` | `current=RULES.CURRENT, resistance=RULES.RESISTANCE` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Solve for single tracking loop operational potential drops via Ohm's relation ($V = I R$). Project: Project Formulon-Physics |
| `electromagnetism` | `resistance_from_resistivity(resistivity_rho: float, length: float, area: float)` | `resistivity_rho=RULES.RESISTIVITY, length=RULES.DISTANCE, area=RULES.AREA` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Translate uniform material property parameters into absolute component resistances ($R = \\rho L / A$). Project: Project Formulon-Physics |
| `electromagnetism` | `temperature_dependent_resistance(r0: float, alpha_coefficient: float, delta_t: float)` | `r0=RULES.RESISTANCE, alpha_coefficient=RULES.REAL, delta_t=RULES.REAL` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Evaluate linear tracking thermal mutations across component resistance steps ($R(T) = R_0[1 + \\alpha \\Delta T]$). Project: Project Formulon-Physics |
| `electromagnetism` | `resistors_series_equivalent(resistances: list[float])` | `mathematical checks in implementation` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Sum resistances across continuous series circuit paths ($R_{eq} = \\sum R_i$). Project: Project Formulon-Physics |
| `electromagnetism` | `resistors_parallel_equivalent(resistances: list[float])` | `mathematical checks in implementation` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Calculate composite parallel pathway network resistance footprints ($\\frac{1}{R_{eq}} = \\sum \\frac{1}{R_i}$). Project: Project Formulon-Physics |
| `electromagnetism` | `joule_heating_power(voltage: float, current: float)` | `voltage=RULES.VOLTAGE, current=RULES.CURRENT` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Measure electrical energy dissipation rates crossing circuit pathways via Joule's law ($P = V I$). Project: Project Formulon-Physics |
| `electromagnetism` | `magnetic_flux(field_func: Callable[[float, float], float], area: float, theta_deg: float)` | `area=RULES.AREA, theta_deg=RULES.ANGLE_DEG` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Measure surface induction flux profiles scaling vector intersections ($\Phi_B = B A \\cos\\theta$). Project: Project Formulon-Physics |
| `electromagnetism` | `faradays_induction_emf(delta_flux_b: float, delta_t: float)` | `delta_flux_b=RULES.REAL, delta_t=RULES.POSITIVE_TIME` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Calculate induced electromotive potentials tracking flux changes via Faraday's framework ($\\mathcal{E} = -\\Delta\Phi_B / \\Delta t$). Project: Project Formulon-Physics |
| `electromagnetism` | `motional_emf(field_b: float, length: float, velocity: float)` | `field_b=RULES.MAGNETIC_FIELD, length=RULES.DISTANCE, velocity=RULES.SPEED` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Calculate kinetic potential distributions driven along conductors cutting tracking fields ($\\mathcal{E} = B L v$). Project: Project Formulon-Physics |
| `electromagnetism` | `self_inductance_flux(flux_b: float, current: float)` | `flux_b=RULES.REAL, current=RULES.CURRENT` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Evaluate structural induction scale invariants matching loop configurations ($L = \Phi_B / I$). Project: Project Formulon-Physics |
| `electromagnetism` | `inductor_induced_emf(inductance_l: float, delta_i: float, delta_t: float)` | `inductance_l=RULES.REAL, delta_i=RULES.REAL, delta_t=RULES.POSITIVE_TIME` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Calculate inductive reverse potential boundaries bucking active current changes ($\\mathcal{E} = -L \\frac{\\Delta I}{\\Delta t}$). Project: Project Formulon-Physics |
| `electromagnetism` | `inductor_stored_energy(inductance_l: float, current: float)` | `inductance_l=RULES.REAL, current=RULES.CURRENT` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Measure dynamic potential fields stored inside inductive vector arrays ($U = \\frac{1}{2}L I^2$). Project: Project Formulon-Physics |
| `electromagnetism` | `magnetic_field_energy_density(field_b: float)` | `field_b=RULES.MAGNETIC_FIELD` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Evaluate localized specific potential energy density fields bound within induction arrays ($u_B = B^2 / (2\\mu_0)$). Project: Project Formulon-Physics |
| `electromagnetism` | `rlc_series_impedance(resistance: float, x_l: float, x_c: float)` | `resistance=RULES.RESISTANCE, x_l=RULES.REAL, x_c=RULES.REAL` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Calculate net alternate phase vector network impedance tracking limits across AC systems. $Z = \\sqrt{R^2 + (X_L - X_C)^2}$ Project: Project Formulon-Physics |
| `electromagnetism` | `inductive_reactance(angular_frequency_omega: float, inductance_l: float)` | `angular_frequency_omega=RULES.ANGULAR_FREQUENCY, inductance_l=RULES.REAL` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Measure inductive AC tracking loop current phase block metrics ($X_L = \omega L$). Project: Project Formulon-Physics |
| `electromagnetism` | `capacitive_reactance(angular_frequency_omega: float, capacitance_c: float)` | `angular_frequency_omega=RULES.ANGULAR_FREQUENCY, capacitance_c=RULES.REAL` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Measure capacitive AC tracking phase expansion block metrics ($X_C = 1 / (\omega C)$). Project: Project Formulon-Physics |
| `electromagnetism` | `resonant_angular_frequency(inductance_l: float, capacitance_c: float)` | `inductance_l=RULES.REAL, capacitance_c=RULES.REAL` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Return the LC resonant angular frequency, omega_0 = 1/sqrt(LC), in rad/s. |
| `electromagnetism` | `resonant_frequency(inductance_l: float, capacitance_c: float)` | `mathematical checks in implementation` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Return the LC resonant frequency f_0 = 1/(2*pi*sqrt(LC)), in Hz. |
| `electromagnetism` | `maxwell_ampere_displacement_current(i_encl: float, delta_flux_e: float, delta_t: float)` | `i_encl=RULES.CURRENT, delta_flux_e=RULES.REAL, delta_t=RULES.POSITIVE_TIME` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Evaluate continuous loop field circulations driving combined real conduction and dynamic displacement paths. $\oint B \\cdot dl = \\mu_0 \\left(I_{encl} + \\epsilon_0 \\frac{\\Delt |
| `electromagnetism` | `em_wave_speed_vacuum()` | `mathematical checks in implementation` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Return baseline structural phase propagation velocities evaluated for vacuum waves via continuum constants ($c = 1 / \\sqrt{\\mu_0 \\epsilon_0}$). Project: Project Formulon-Physics |
| `electromagnetism` | `poynting_vector_magnitude(field_e: float, field_b: float)` | `field_e=RULES.REAL, field_b=RULES.MAGNETIC_FIELD` | Electrical engineering, sensors, circuits, electrostatics, magnetic devices and field estimates. Measure directional electromagnetic energy flux power transfer throughput dimensions ($S = E B / \\mu_0$). Project: Project Formulon-Physics |
| `fluidmechanics` | `density(mass: float, volume: float)` | `mass=RULES.MASS, volume=RULES.VOLUME` | Pipe flow, hydraulics, drag, buoyancy and continuum-fluid estimates. Calculate the mass density of a uniform substance ($ ho = m/V$). Project: Project Formulon-Physics Parameters ---------- mass : float The total mass of the body or fluid sample in  |
| `fluidmechanics` | `pressure(force: float, area: float)` | `force=RULES.FORCE, area=RULES.AREA` | Pipe flow, hydraulics, drag, buoyancy and continuum-fluid estimates. Calculate the average perpendicular pressure acting on a surface ($P = F_\perp/A$). Project: Project Formulon-Physics Parameters ---------- force : float The magnitude of the norma |
| `fluidmechanics` | `hydrostatic_pressure(density: float, g: float, depth: float)` | `density=RULES.DENSITY, g=RULES.GRAVITY, depth=RULES.HEIGHT` | Pipe flow, hydraulics, drag, buoyancy and continuum-fluid estimates. Calculate the gauge hydrostatic pressure at a specific depth within a fluid ($P = ho g h$). Project: Project Formulon-Physics Parameters ---------- density : float The mass density |
| `fluidmechanics` | `buoyant_force(fluid_density: float, volume_submerged: float, g: float)` | `fluid_density=RULES.DENSITY, volume_submerged=RULES.VOLUME, g=RULES.GRAVITY` | Pipe flow, hydraulics, drag, buoyancy and continuum-fluid estimates. Calculate the upward buoyant force acting on a submerged body ($F_b = ho_f V_{sub} g$). Project: Project Formulon-Physics Parameters ---------- fluid_density : float The mass densi |
| `fluidmechanics` | `continuity_equation(area1: float, velocity1: float, area2: float)` | `area1=RULES.AREA, velocity1=RULES.SPEED, area2=RULES.AREA` | Pipe flow, hydraulics, drag, buoyancy and continuum-fluid estimates. Solve for the output velocity using the Continuity Equation ($A_1 v_1 = A_2 v_2$). Assumes steady, continuous, incompressible, non-viscous fluid stream tracking. Project: Project F |
| `fluidmechanics` | `bernoullis_equation(p1: float, density: float, v1: float, y1: float, v2: float, y2: float, g: float)` | `p1=RULES.PRESSURE, density=RULES.DENSITY, v1=RULES.SPEED, y1=RULES.HEIGHT, v2=RULES.SPEED, y2=RULES.HEIGHT, g=RULES.GRAVITY` | Pipe flow, hydraulics, drag, buoyancy and continuum-fluid estimates. Determine the local pressure at a downstream state using Bernoulli's Theorem. $P_2 = P_1 + \\frac{1}{2}\\rho(v_1^2 - v_2^2) + \\rho g(y_1 - y_2)$ Project: Project Formulon-Physics  |
| `fluidmechanics` | `torricellis_law(g: float, liquid_height: float)` | `g=RULES.GRAVITY, liquid_height=RULES.HEIGHT` | Pipe flow, hydraulics, drag, buoyancy and continuum-fluid estimates. Calculate the theoretical speed of efflux from an open sharp-edged orifice via Torricelli's Law ($v = \sqrt{2gh}$). Project: Project Formulon-Physics Parameters ---------- g : floa |
| `fluidmechanics` | `reynolds_number(density: float, flow_speed: float, characteristic_length: float, viscosity: float)` | `density=RULES.DENSITY, flow_speed=RULES.SPEED, characteristic_length=RULES.DISTANCE, viscosity=RULES.VISCOSITY` | Pipe flow, hydraulics, drag, buoyancy and continuum-fluid estimates. Calculate the dimensionless Reynolds number evaluating fluid turbulence scales ($Re = \frac{\rho v L}{\mu}$). Project: Project Formulon-Physics Parameters ---------- density : floa |
| `fluidmechanics` | `stress(force: float, cross_sectional_area: float)` | `force=RULES.FORCE, cross_sectional_area=RULES.AREA` | Pipe flow, hydraulics, drag, buoyancy and continuum-fluid estimates. Calculate the internal restorative mechanical stress distribution ($\sigma = F/A$). Project: Project Formulon-Physics Parameters ---------- force : float The deforming force payloa |
| `fluidmechanics` | `strain(delta_l: float, initial_l: float)` | `delta_l=RULES.DISTANCE, initial_l=RULES.DISTANCE` | Pipe flow, hydraulics, drag, buoyancy and continuum-fluid estimates. Calculate the structural dimensionless linear deformation strain value ($\epsilon = \Delta L / L_0$). Project: Project Formulon-Physics Parameters ---------- delta_l : float The ab |
| `fluidmechanics` | `youngs_modulus(stress: float, strain: float)` | `stress=RULES.REAL, strain=RULES.REAL` | Pipe flow, hydraulics, drag, buoyancy and continuum-fluid estimates. Calculate the intrinsic Young's Modulus of elasticity tracking tensile rigidity ($E = \sigma / \epsilon$). Project: Project Formulon-Physics Parameters ---------- stress : float Th |
| `fluidmechanics` | `shear_modulus(shear_stress: float, shear_strain: float)` | `shear_stress=RULES.REAL, shear_strain=RULES.REAL` | Pipe flow, hydraulics, drag, buoyancy and continuum-fluid estimates. Calculate the material Shear Modulus evaluation index ($G = \tau / \gamma$). Project: Project Formulon-Physics Parameters ---------- shear_stress : float The applied planar or tang |
| `fluidmechanics` | `bulk_modulus(delta_pressure: float, volumetric_strain: float)` | `delta_pressure=RULES.REAL, volumetric_strain=RULES.REAL` | Pipe flow, hydraulics, drag, buoyancy and continuum-fluid estimates. Calculate the intrinsic compression Bulk Modulus measuring volume elasticity resistances ($K = - \Delta P / (\Delta V / V_0)$). Project: Project Formulon-Physics Parameters ------- |
| `fluidmechanics` | `surface_tension(force: float, perimeter_length: float)` | `force=RULES.FORCE, perimeter_length=RULES.DISTANCE` | Pipe flow, hydraulics, drag, buoyancy and continuum-fluid estimates. Determine the acting boundary layer liquid surface tension property ($\gamma = F / L$). Project: Project Formulon-Physics Parameters ---------- force : float The active contractive |
| `fluidmechanics` | `capillary_rise(surface_tension: float, contact_angle_deg: float, tube_radius: float, fluid_density: float, g: float)` | `surface_tension=RULES.POSITIVE, contact_angle_deg=RULES.ANGLE_DEG_90, tube_radius=RULES.RADIUS, fluid_density=RULES.DENSITY, g=RULES.GRAVITY` | Pipe flow, hydraulics, drag, buoyancy and continuum-fluid estimates. Predict the static capillary equilibrium tube height rise or depression level. $h = \\frac{2 \gamma \cos(\\theta)}{\\rho g r}$ Project: Project Formulon-Physics Parameters -------- |
| `fluidmechanics` | `stokes_law(viscosity: float, radius: float, terminal_velocity: float)` | `viscosity=RULES.VISCOSITY, radius=RULES.RADIUS, terminal_velocity=RULES.SPEED` | Pipe flow, hydraulics, drag, buoyancy and continuum-fluid estimates. Calculate the viscous drag resistance profile acting over an isolated sphere via Stokes' Law ($F_d = 6 \pi \mu r v$). Valid inside laminar regimes with very low particle Reynolds l |
| `fluidmechanics` | `poiseuilles_law(pressure_drop: float, pipe_radius: float, pipe_length: float, fluid_viscosity: float)` | `pressure_drop=RULES.REAL, pipe_radius=RULES.RADIUS, pipe_length=RULES.DISTANCE, fluid_viscosity=RULES.VISCOSITY` | Pipe flow, hydraulics, drag, buoyancy and continuum-fluid estimates. Calculate the laminar volumetric flow rate inside a cylindrical pipe channel via Poiseuille's Law. $Q = \\frac{\pi \Delta P r^4}{8 \mu L}$ Project: Project Formulon-Physics Paramet |
| `mathematicalphysics` | `safe_compute_jit(use_jit: bool=True, nopython: bool=False)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Combined decorator for global engineering error handling and numba JIT compilation. Project: Project Formulon-Physics Parameters ---------- use_jit : bool, default=True Whether to  |
| `mathematicalphysics` | `safe_compute(func)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Legacy architectural decorator - maps directly to the unified safe_compute_jit pipeline. |
| `mathematicalphysics` | `validate_values(values)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Validate numerical inputs to ensure boundary compatibility with physics compute limits. Project: Project Formulon-Physics |
| `mathematicalphysics` | `validate_string(s: str)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Sanitize input expressions or structural strings against processing injection boundaries. Project: Project Formulon-Physics |
| `mathematicalphysics` | `validate_zeros_negatives(values)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Verify input parameters are strictly positive to preserve matrix and division stability. Project: Project Formulon-Physics |
| `mathematicalphysics` | `safe_int_float(value)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Safely convert type configurations into standardized physics numerical types. Project: Project Formulon-Physics |
| `mathematicalphysics` | `fourier_series(x: float)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Compute the real Fourier series evaluation approximation of a target function at point x. Project: Project Formulon-Physics |
| `mathematicalphysics` | `complex_fourier_series(x: float)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Evaluate the complex exponential form of the Fourier series representation at point x. Project: Project Formulon-Physics |
| `mathematicalphysics` | `fourier_transform(f: callable, x: float)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Compute the integral Fourier transform of the continuous function f evaluated at point x. Project: Project Formulon-Physics |
| `mathematicalphysics` | `inverse_fourier_transform(F: callable, t: float)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Map a frequency spectrum function back to the coordinate spacetime domain at localized point t. Project: Project Formulon-Physics |
| `mathematicalphysics` | `parsevals_identity(f: callable, x: float)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Compute energy conservation parameters via Parseval's identity integration spaces. Project: Project Formulon-Physics |
| `mathematicalphysics` | `convolution(f: callable, g: callable, x: float)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Compute the mathematical spatial overlap convolution of two field profiles at point x. Project: Project Formulon-Physics |
| `mathematicalphysics` | `legendre_polynomial(n: int, x: float)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Evaluate the Legendre Polynomial $P_n(x)$ mapping angular momentum solutions. Project: Project Formulon-Physics |
| `mathematicalphysics` | `laguerre_polynomial(n: int, x: float)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Evaluate the Laguerre Polynomial $L_n(x)$ tracking radial quantum wavefunctions. Project: Project Formulon-Physics |
| `mathematicalphysics` | `hermite_polynomial(n: int, x: float)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Evaluate the Hermite Polynomial $H_n(x)$ resolving quantum harmonic oscillators. Project: Project Formulon-Physics |
| `mathematicalphysics` | `bessel_function(n: int, x: float)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Evaluate the Cylindrical Bessel function of the first kind J_n(x). Note: intentionally NOT numba-JIT'd. JIT'ing a thin SciPy wrapper only compiles to object-mode with zero speed be |
| `mathematicalphysics` | `spherical_bessel(n: int, x: float)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Evaluate the Spherical Bessel function of the first kind j_n(x). Note: intentionally NOT numba-JIT'd for the same reason as bessel_function above -- it's a direct SciPy delegation, |
| `mathematicalphysics` | `heat_equation(initial_condition: callable, x: float, t: float)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Resolve temporal diffusion distributions by computing solutions to the classic Heat Equation. Project: Project Formulon-Physics |
| `mathematicalphysics` | `wave_equation(initial_displacement: callable, initial_velocity: callable, x: float, t: float)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Resolve localized scalar wave field propagation solutions under the second-order Wave Equation. Project: Project Formulon-Physics |
| `mathematicalphysics` | `solve_ode(equation: callable, initial_conditions, x: float)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Solve systems of ordinary differential equations tracking multi-state initial value conditions via RK45. Project: Project Formulon-Physics |
| `mathematicalphysics` | `linear_algebra(matrix)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Execute eigenvalue systems decompose mapping and singular value structural decomposition profiles. Project: Project Formulon-Physics |
| `mathematicalphysics` | `gauss_divergence(vector_field: callable, surface: callable, limits: list)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Compute vector flux indicators through an enclosed boundary space using Gauss's Divergence Theorem. Project: Project Formulon-Physics |
| `mathematicalphysics` | `stokes(vector_field: callable, curve: callable, surface: callable, limits: list)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Compute closed pathway vector loop circulation bounds utilizing Stokes' Theorem tracking. Project: Project Formulon-Physics |
| `mathematicalphysics` | `greens(vector_field: callable, curve: callable, limits: list)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Evaluate planar vector boundaries flow conditions using Green's Theorem. Project: Project Formulon-Physics |
| `mathematicalphysics` | `tensor(tensor1, tensor2)` | `mathematical checks in implementation` | Mathematical methods used in differential equations, transforms and special functions. Perform multidimensional array contractions, additions, and metric inner tensor operations. Project: Project Formulon-Physics |
| `mathematicalseries` | `arithmetic_nterm(a1, d, n)` | `mathematical checks in implementation` | Series, sequences, sums and numerical-analysis building blocks. Determine the value of the specific n-th term within an arithmetic progression progression matrix. Project: Project Formulon-Physics |
| `mathematicalseries` | `arithmetic_sum(a1, d, n)` | `mathematical checks in implementation` | Series, sequences, sums and numerical-analysis building blocks. Calculate the mathematical accumulation sum up to the target n-th term inside an arithmetic progression. Project: Project Formulon-Physics |
| `mathematicalseries` | `geometric_nterm(a1, r, n)` | `mathematical checks in implementation` | Series, sequences, sums and numerical-analysis building blocks. Determine the value of the specific n-th term within an isolated geometric sequence context. Project: Project Formulon-Physics |
| `mathematicalseries` | `infinite_geometric_sum(a1, r)` | `mathematical checks in implementation` | Series, sequences, sums and numerical-analysis building blocks. Evaluate convergent limit spaces to map the sum of an infinite geometric progression string. Project: Project Formulon-Physics |
| `mathematicalseries` | `natural_numbers_sum(n)` | `mathematical checks in implementation` | Series, sequences, sums and numerical-analysis building blocks. Sum all continuous integers starting from unity up to the targeted parameter marker count n. Project: Project Formulon-Physics |
| `mathematicalseries` | `squares_sum(n)` | `mathematical checks in implementation` | Series, sequences, sums and numerical-analysis building blocks. Evaluate cumulative sum structures across consecutive integer squares tracking up to index position n. Project: Project Formulon-Physics |
| `mathematicalseries` | `cubes_sum(n)` | `mathematical checks in implementation` | Series, sequences, sums and numerical-analysis building blocks. Evaluate cumulative sum structures across consecutive integer cubic parameters tracking up to index position n. Project: Project Formulon-Physics |
| `mathematicalseries` | `odd_numbers_sum(n)` | `mathematical checks in implementation` | Series, sequences, sums and numerical-analysis building blocks. Accumulate sequential mathematical evaluations across the initial sequence set of n explicit odd number profiles. Project: Project Formulon-Physics |
| `mathematicalseries` | `fibonacci_nterm(n)` | `mathematical checks in implementation` | Series, sequences, sums and numerical-analysis building blocks. Solve for and isolate the structural n-th term positional value inside the standard Fibonacci recurrence chain. Project: Project Formulon-Physics |
| `modernphysics` | `lorentz_factor(v: float)` | `v=RULES.SPEED` | Relativity, photons, quantum-scale estimates, nuclear decay and particle physics. Calculate the dimensionless Lorentz factor scaling property ($\gamma = 1 / \sqrt{1 - v^2/c^2}$). Project: Project Formulon-Physics |
| `modernphysics` | `time_dilation(t0_proper: float, v: float)` | `t0_proper=RULES.NON_NEG_TIME, v=RULES.SPEED` | Relativity, photons, quantum-scale estimates, nuclear decay and particle physics. Calculate dilated timeline horizons observed from a moving frame ($\Delta t = \gamma \Delta t_0$). Project: Project Formulon-Physics |
| `modernphysics` | `length_contraction(l0_proper: float, v: float)` | `l0_proper=RULES.DISTANCE, v=RULES.SPEED` | Relativity, photons, quantum-scale estimates, nuclear decay and particle physics. Evaluate dimensional length contraction scales along the axis of relative motion ($L = L_0 / \gamma$). Project: Project Formulon-Physics |
| `modernphysics` | `relativistic_momentum(m0_rest: float, v: float)` | `m0_rest=RULES.MASS, v=RULES.SPEED` | Relativity, photons, quantum-scale estimates, nuclear decay and particle physics. Evaluate net relativistic momentum thresholds across high velocity states ($p = \gamma m_0 v$). Project: Project Formulon-Physics |
| `modernphysics` | `total_relativistic_energy(m0_rest: float, v: float)` | `m0_rest=RULES.MASS, v=RULES.SPEED` | Relativity, photons, quantum-scale estimates, nuclear decay and particle physics. Determine the comprehensive total relativistic energy profile ($E = \gamma m_0 c^2$). Project: Project Formulon-Physics |
| `modernphysics` | `rest_mass_energy(m0_rest: float)` | `m0_rest=RULES.MASS` | Relativity, photons, quantum-scale estimates, nuclear decay and particle physics. Determine localized rest mass state equivalence footprints ($E_0 = m_0 c^2$). Project: Project Formulon-Physics |
| `modernphysics` | `energy_momentum_invariant(momentum_p: float, m0_rest: float)` | `momentum_p=RULES.REAL, m0_rest=RULES.MASS` | Relativity, photons, quantum-scale estimates, nuclear decay and particle physics. Resolve total energy values using the relativistic energy-momentum invariant framework ($E = \sqrt{(pc)^2 + (m_0c^2)^2}$). Project: Project Formulon-Physics |
| `modernphysics` | `relativistic_velocity_addition(u_velocity: float, v_frame: float)` | `u_velocity=RULES.REAL, v_frame=RULES.REAL` | Relativity, photons, quantum-scale estimates, nuclear decay and particle physics. Calculate the combined frame transformations for relativistic velocities ($u' = (u - v) / (1 - uv/c^2)$). Project: Project Formulon-Physics |
| `modernphysics` | `photon_energy(wavelength: float)` | `wavelength=RULES.WAVELENGTH` | Relativity, photons, quantum-scale estimates, nuclear decay and particle physics. Determine single photon operational energy capacities using spectral parameters ($E = hc / \lambda$). Project: Project Formulon-Physics |
| `modernphysics` | `photoelectric_max_kinetic_energy(wavelength: float, work_function_ev: float)` | `wavelength=RULES.WAVELENGTH, work_function_ev=RULES.REAL` | Relativity, photons, quantum-scale estimates, nuclear decay and particle physics. Calculate peak electron emission kinetic energy fields tracking the photoelectric effect. $K_{max} = \frac{hc}{\lambda} - \Phi$ Project: Project Formulon-Physics |
| `modernphysics` | `de_broglie_wavelength(momentum_p: float)` | `momentum_p=RULES.REAL` | Relativity, photons, quantum-scale estimates, nuclear decay and particle physics. Calculate corresponding de Broglie spatial wavelengths matching matter momentum states ($\lambda = h / p$). Project: Project Formulon-Physics |
| `modernphysics` | `heisenberg_uncertainty_minimum_momentum(delta_x_position: float)` | `delta_x_position=RULES.DISTANCE` | Relativity, photons, quantum-scale estimates, nuclear decay and particle physics. Isolate standard minimal momentum constraint profiles via Heisenberg's uncertainty relationship ($\Delta p \ge \hbar / (2 \Delta x)$). Project: Project Formulon-Physics |
| `modernphysics` | `bohr_orbit_radius_angular_momentum(principal_n: int)` | `principal_n=RULES.DEGREES_FREEDOM` | Relativity, photons, quantum-scale estimates, nuclear decay and particle physics. Determine quantized angular momentum thresholds across circular orbital systems ($L = n \hbar$). Project: Project Formulon-Physics |
| `modernphysics` | `bohr_hydrogen_energy_level(principal_n: int)` | `principal_n=RULES.DEGREES_FREEDOM` | Relativity, photons, quantum-scale estimates, nuclear decay and particle physics. Predict structural energy level tracks in Hydrogen atoms under the Bohr approximation ($E_n = -13.6 ext{ eV} / n^2$). Project: Project Formulon-Physics |
| `modernphysics` | `compton_scattering_wavelength_shift(scattering_theta_deg: float)` | `scattering_theta_deg=RULES.ANGLE_DEG` | Relativity, photons, quantum-scale estimates, nuclear decay and particle physics. Calculate electromagnetic wavelength shift profiles under localized particle collisions. $\Delta\lambda = \frac{h}{m_e c}(1 - \cos\theta)$ Project: Project Formulon-Physics |
| `modernphysics` | `radioactive_decay_remaining_nuclei(n0_initial: float, decay_constant_lambda: float, t_elapsed: float)` | `n0_initial=RULES.REAL, decay_constant_lambda=RULES.NON_NEGATIVE, t_elapsed=RULES.NON_NEG_TIME` | Relativity, photons, quantum-scale estimates, nuclear decay and particle physics. Calculate remaining structural nuclei records tracking continuous exponential decay sequences ($N(t) = N_0 e^{-\lambda t}$). Project: Project Formulon-Physics |
| `modernphysics` | `half_life_from_decay_constant(decay_constant_lambda: float)` | `decay_constant_lambda=RULES.NON_NEGATIVE` | Relativity, photons, quantum-scale estimates, nuclear decay and particle physics. Map characteristic structural system half-lives using raw isotopic decay parameters ($T_{1/2} = \ln(2) / \lambda$). Project: Project Formulon-Physics |
| `modernphysics` | `binding_energy_from_mass_defect(mass_defect_kg: float)` | `mass_defect_kg=RULES.REAL` | Relativity, photons, quantum-scale estimates, nuclear decay and particle physics. Translate raw structural mass missing differentials directly into nuclear binding holding forces ($E_{be} = \Delta m \cdot c^2$). Project: Project Formulon-Physics |
| `thermodynamics` | `linear_expansion(alpha: float, l0: float, delta_t: float)` | `alpha=RULES.REAL, l0=RULES.DISTANCE, delta_t=RULES.REAL` | Thermal systems, engines, heat exchangers, gases and energy balances. Calculate linear spatial elongation under changing temperature states ($\Delta L = \alpha L_0 \Delta T$). Project: Project Formulon-Physics |
| `thermodynamics` | `volume_expansion(beta: float, v0: float, delta_t: float)` | `beta=RULES.REAL, v0=RULES.VOLUME, delta_t=RULES.REAL` | Thermal systems, engines, heat exchangers, gases and energy balances. Calculate volumetric expansion matching continuous thermal adjustments ($\Delta V = \beta V_0 \Delta T$). Project: Project Formulon-Physics |
| `thermodynamics` | `sensible_heat(mass: float, specific_heat: float, delta_t: float)` | `mass=RULES.MASS, specific_heat=RULES.SPECIFIC_HEAT, delta_t=RULES.REAL` | Thermal systems, engines, heat exchangers, gases and energy balances. Evaluate heat energy exchanges driving single-phase thermal delta shifts ($Q = m c \Delta T$). Project: Project Formulon-Physics |
| `thermodynamics` | `latent_heat(mass: float, specific_latent_heat: float)` | `mass=RULES.MASS, specific_latent_heat=RULES.LATENT_HEAT` | Thermal systems, engines, heat exchangers, gases and energy balances. Evaluate enthalpy configurations driving physical pure isothermal phase variations ($Q = m L$). Project: Project Formulon-Physics |
| `thermodynamics` | `ideal_gas_pressure(moles: float, temperature: float, volume: float)` | `moles=RULES.MOLES, temperature=RULES.TEMPERATURE, volume=RULES.VOLUME` | Thermal systems, engines, heat exchangers, gases and energy balances. Solve for ideal gas state pressures using thermodynamic parameter matching ($P = nRT / V$). Project: Project Formulon-Physics |
| `thermodynamics` | `van_der_waals_pressure(moles: float, volume: float, temperature: float, a_factor: float, b_factor: float)` | `moles=RULES.MOLES, volume=RULES.VOLUME, temperature=RULES.TEMPERATURE, a_factor=RULES.NON_NEGATIVE, b_factor=RULES.NON_NEGATIVE` | Thermal systems, engines, heat exchangers, gases and energy balances. Predict non-ideal real gas state pressures accounting for intermolecular attractions and co-volumes. $P = \\frac{nRT}{V - nb} - \\frac{an^2}{V^2}$ Project: Project Formulon-Physics |
| `thermodynamics` | `average_molecular_kinetic_energy(temperature: float)` | `temperature=RULES.TEMPERATURE` | Thermal systems, engines, heat exchangers, gases and energy balances. Determine specific mean localized molecular translation kinetic energy footprints ($K_{avg} = \\frac{3}{2}k_B T$). Project: Project Formulon-Physics |
| `thermodynamics` | `rms_velocity(temperature: float, molar_mass: float)` | `temperature=RULES.TEMPERATURE, molar_mass=RULES.MOLAR_MASS` | Thermal systems, engines, heat exchangers, gases and energy balances. Calculate root-mean-square thermal velocities tracking particle metrics ($v_{rms} = \\sqrt{3RT / M}$). Project: Project Formulon-Physics |
| `thermodynamics` | `mean_free_path(molecular_diameter: float, number_density: float)` | `molecular_diameter=RULES.DISTANCE, number_density=RULES.NUMBER_DENSITY` | Thermal systems, engines, heat exchangers, gases and energy balances. Calculate average displacement tracking scales between successive collision events ($\lambda = \\frac{1}{\\sqrt{2}\pi d^2 n}$). Project: Project Formulon-Physics |
| `thermodynamics` | `first_law_internal_energy(heat_added: float, work_done: float)` | `heat_added=RULES.REAL, work_done=RULES.REAL` | Thermal systems, engines, heat exchangers, gases and energy balances. Express energy balance tracking matching classical First Law limits ($dU = dQ - dW$). Project: Project Formulon-Physics |
| `thermodynamics` | `work_variable_volume(pressure_func: Callable[[float], float], v_start: float, v_end: float)` | `mathematical checks in implementation` | Thermal systems, engines, heat exchangers, gases and energy balances. Integrate dynamic boundary variations determining work distributions ($W = \\int P(V) dV$). Project: Project Formulon-Physics |
| `thermodynamics` | `work_isothermal(moles: float, temperature: float, v1: float, v2: float)` | `moles=RULES.MOLES, temperature=RULES.TEMPERATURE, v1=RULES.VOLUME, v2=RULES.VOLUME` | Thermal systems, engines, heat exchangers, gases and energy balances. Calculate mechanical work deliveries completed along pure isothermal limits ($W = nRT \\ln(V_2 / V_1)$). Project: Project Formulon-Physics |
| `thermodynamics` | `adiabatic_process_constant(pressure: float, volume: float, gamma: float)` | `pressure=RULES.PRESSURE, volume=RULES.VOLUME, gamma=RULES.ADIABATIC_INDEX` | Thermal systems, engines, heat exchangers, gases and energy balances. Isolate invariant pathway tracking indexes matching zero-heat transfer horizons ($C = P V^\\gamma$). Project: Project Formulon-Physics |
| `thermodynamics` | `work_adiabatic(p1: float, v1: float, p2: float, v2: float, gamma: float)` | `p1=RULES.PRESSURE, v1=RULES.VOLUME, p2=RULES.PRESSURE, v2=RULES.VOLUME, gamma=RULES.ADIABATIC_INDEX` | Thermal systems, engines, heat exchangers, gases and energy balances. Solve for energetic work translations performed inside non-isentropic expansion boundaries. $W = \\frac{P_1 V_1 - P_2 V_2}{\\gamma - 1}$ Project: Project Formulon-Physics |
| `thermodynamics` | `mayers_relation_cp(cv_molar: float)` | `cv_molar=RULES.SPECIFIC_HEAT` | Thermal systems, engines, heat exchangers, gases and energy balances. Map constant pressure specific capacities using Mayer's ideal relation ($C_p = C_v + R$). Project: Project Formulon-Physics |
| `thermodynamics` | `adiabatic_index_from_degrees_freedom(degrees_freedom: int)` | `degrees_freedom=RULES.DEGREES_FREEDOM` | Thermal systems, engines, heat exchangers, gases and energy balances. Evaluate structural molecular heat capacity ratio properties tracking degrees of freedom ($\\gamma = 1 + 2/f$). Project: Project Formulon-Physics |
| `thermodynamics` | `carnot_efficiency(t_cold: float, t_hot: float)` | `t_cold=RULES.TEMPERATURE, t_hot=RULES.TEMPERATURE` | Thermal systems, engines, heat exchangers, gases and energy balances. Measure absolute theoretical thermodynamic efficiency conversion limits across heat engines ($\\eta = 1 - T_C / T_H$). Project: Project Formulon-Physics |
| `thermodynamics` | `entropy_change(heat_reversible: float, temperature: float)` | `heat_reversible=RULES.REAL, temperature=RULES.TEMPERATURE` | Thermal systems, engines, heat exchangers, gases and energy balances. Measure state degradation parameter mutations matching specific heat additions ($dS = dQ_{rev} / T$). Project: Project Formulon-Physics |
| `thermodynamics` | `boltzmann_entropy(microstates_w: float)` | `microstates_w=RULES.MICROSTATES` | Thermal systems, engines, heat exchangers, gases and energy balances. Determine statistical entropy values from microscopic configuration multiplicity bounds ($S = k_B \\ln W$). Project: Project Formulon-Physics |
| `thermodynamics` | `thermal_conduction_rate(k_conduct: float, area: float, delta_t: float, thickness_dx: float)` | `k_conduct=RULES.THERMAL_CONDUCTIVITY, area=RULES.AREA, delta_t=RULES.REAL, thickness_dx=RULES.DISTANCE` | Thermal systems, engines, heat exchangers, gases and energy balances. Measure directional linear conductive heat energy flow fluxes utilizing Fourier's framework ($dQ/dt = -k A \\frac{\\Delta T}{\\Delta x}$). Project: Project Formulon-Physics |
| `thermodynamics` | `stefan_boltzmann_radiation(emissivity: float, area: float, temperature: float)` | `emissivity=RULES.EMISSIVITY, area=RULES.AREA, temperature=RULES.TEMPERATURE` | Thermal systems, engines, heat exchangers, gases and energy balances. Calculate macroscopic ideal blackbody electromagnetic energetic discharge radiation power ($P = \\epsilon \\sigma A T^4$). Project: Project Formulon-Physics |
| `thermodynamics` | `wiens_displacement_wavelength(temperature: float)` | `temperature=RULES.TEMPERATURE` | Thermal systems, engines, heat exchangers, gases and energy balances. Isolate peak spectral emission wavelengths matching specific temperature states via Wien's relation ($\\lambda_{max} = b / T$). Project: Project Formulon-Physics |
| `thermodynamics` | `newtons_cooling_rate(cooling_constant_k: float, t_body: float, t_ambient: float)` | `cooling_constant_k=RULES.NON_NEGATIVE, t_body=RULES.TEMPERATURE, t_ambient=RULES.TEMPERATURE` | Thermal systems, engines, heat exchangers, gases and energy balances. Predict rate tracking parameters governing convective temperature drop states ($dT/dt = -K(T_{body} - T_{ambient})$). Project: Project Formulon-Physics |
| `wavedynamics` | `shm_acceleration_from_displacement(omega: float, x: float)` | `omega=RULES.ANGULAR_FREQUENCY, x=RULES.DISPLACEMENT` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Calculate SHM acceleration directly from the differential equation constraint ($a = -\omega^2 x$). Project: Project Formulon-Physics |
| `wavedynamics` | `shm_displacement(amplitude: float, omega: float, t: float, phi_rad: float)` | `amplitude=RULES.AMPLITUDE, omega=RULES.ANGULAR_FREQUENCY, t=RULES.NON_NEG_TIME, phi_rad=RULES.REAL` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Calculate system displacement tracking position state in SHM ($x(t) = A \cos(\omega t + \phi)$). Project: Project Formulon-Physics |
| `wavedynamics` | `shm_velocity(amplitude: float, omega: float, t: float, phi_rad: float)` | `amplitude=RULES.AMPLITUDE, omega=RULES.ANGULAR_FREQUENCY, t=RULES.NON_NEG_TIME, phi_rad=RULES.REAL` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Calculate structural phase space velocity tracking in SHM ($v(t) = -A \omega \sin(\omega t + \phi)$). Project: Project Formulon-Physics |
| `wavedynamics` | `shm_velocity_from_displacement(omega: float, amplitude: float, x: float)` | `omega=RULES.ANGULAR_FREQUENCY, amplitude=RULES.AMPLITUDE, x=RULES.DISPLACEMENT` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Calculate the velocity magnitude given a localized spatial displacement coordinate ($v = \pm\omega\sqrt{A^2 - x^2}$). Project: Project Formulon-Physics |
| `wavedynamics` | `shm_acceleration(amplitude: float, omega: float, t: float, phi_rad: float)` | `amplitude=RULES.AMPLITUDE, omega=RULES.ANGULAR_FREQUENCY, t=RULES.NON_NEG_TIME, phi_rad=RULES.REAL` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Calculate dynamic structural linear acceleration tracking in SHM ($a(t) = -A \omega^2 \cos(\omega t + \phi)$). Project: Project Formulon-Physics |
| `wavedynamics` | `spring_angular_frequency(k: float, mass: float)` | `k=RULES.SPRING_CONST, mass=RULES.MASS` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Evaluate structural natural angular frequency for ideal elastic spring systems ($\omega = \sqrt{k/m}$). Project: Project Formulon-Physics |
| `wavedynamics` | `spring_period(mass: float, k: float)` | `mass=RULES.MASS, k=RULES.SPRING_CONST` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Calculate natural structural oscillation time period of an elastic spring model ($T = 2\pi\sqrt{m/k}$). Project: Project Formulon-Physics |
| `wavedynamics` | `pendulum_period(length: float, g: float)` | `length=RULES.DISTANCE, g=RULES.GRAVITY` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Calculate the small-angle time oscillation period of an idealized simple pendulum ($T = 2\pi\sqrt{L/g}$). Project: Project Formulon-Physics |
| `wavedynamics` | `physical_pendulum_period(i: float, mass: float, g: float, d: float)` | `i=RULES.MOMENT_OF_INERTIA, mass=RULES.MASS, g=RULES.GRAVITY, d=RULES.DISTANCE` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Evaluate mechanical oscillation periods for rigid distributed physical pendulum systems ($T = 2\pi\sqrt{I / (mgd)}$). Project: Project Formulon-Physics |
| `wavedynamics` | `shm_total_energy(k: float, amplitude: float)` | `k=RULES.SPRING_CONST, amplitude=RULES.AMPLITUDE` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Determine invariant consolidated total mechanical energy bound within an active SHM system ($E = rac{1}{2}k A^2$). Project: Project Formulon-Physics |
| `wavedynamics` | `wave_speed(frequency: float, wavelength: float)` | `frequency=RULES.FREQUENCY, wavelength=RULES.WAVELENGTH` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Calculate characteristic phase velocity propagation speeds ($v = f \lambda$). Project: Project Formulon-Physics |
| `wavedynamics` | `wave_number(wavelength: float)` | `wavelength=RULES.WAVELENGTH` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Calculate spatial propagation angular wavenumber constraints ($k = 2\pi / \lambda$). Project: Project Formulon-Physics |
| `wavedynamics` | `string_wave_speed(tension: float, linear_density: float)` | `tension=RULES.FORCE, linear_density=RULES.LINEAR_DENSITY` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Evaluate structural transverse elastic wave transmission speeds sustained across ideal strings ($v = \sqrt{T/\mu}$). Project: Project Formulon-Physics |
| `wavedynamics` | `gas_sound_speed(gamma: float, r_gas: float, temperature: float, molar_mass: float)` | `gamma=RULES.ADIABATIC_INDEX, r_gas=RULES.GAS_CONSTANT, temperature=RULES.TEMPERATURE, molar_mass=RULES.MOLAR_MASS` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Predict acoustic compression wave velocities inside gases using Laplace's formulation ($v = \sqrt{\gamma R T / M}$). Project: Project Formulon-Physics |
| `wavedynamics` | `sound_intensity_level(intensity: float, base_intensity: float)` | `intensity=RULES.SOUND_INTENSITY, base_intensity=RULES.SOUND_INTENSITY_BASE` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Translate pure surface vector acoustic fluxes into standard decibel scaling metrics ($\beta = 10 \log_{10}(I / I_0)$). Project: Project Formulon-Physics |
| `wavedynamics` | `doppler_effect(f_source: float, v_medium: float, v_observer: float, v_source: float, observer_moving_towards: bool, source_moving_towards: bool)` | `f_source=RULES.FREQUENCY, v_medium=RULES.SPEED, v_observer=RULES.SPEED, v_source=RULES.SPEED` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Calculate observed shifting frequencies matching relative motion kinematics under the Doppler phenomenon. $f' = f \left(\frac{v \pm v_o}{v \mp v_s}\right)$ Project: Project Formulo |
| `wavedynamics` | `beat_frequency(f1: float, f2: float)` | `f1=RULES.FREQUENCY, f2=RULES.FREQUENCY` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Isolate envelope modulations generated by dual superposed wave interference grids ($f_{beat} = \|f_1 - f_2\|$). Project: Project Formulon-Physics |
| `wavedynamics` | `open_pipe_fundamental(v_sound: float, length: float)` | `v_sound=RULES.SPEED, length=RULES.DISTANCE` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Calculate the fundamental acoustic resonance frequency matching symmetric open acoustic boundaries ($f = v / 2L$). Project: Project Formulon-Physics |
| `wavedynamics` | `closed_pipe_fundamental(v_sound: float, length: float)` | `v_sound=RULES.SPEED, length=RULES.DISTANCE` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Calculate fundamental resonance modes inside asymmetric closed-ended acoustic boundary lines ($f = v / 4L$). Project: Project Formulon-Physics |
| `wavedynamics` | `refractive_index(c_vacuum: float, v_medium: float)` | `c_vacuum=RULES.SPEED, v_medium=RULES.SPEED` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Measure electromagnetic spatial phase restriction indices across media boundaries ($n = c / v$). Project: Project Formulon-Physics |
| `wavedynamics` | `snells_law_angle(n1: float, n2: float, theta1_deg: float)` | `n1=RULES.REFRACTIVE_INDEX, n2=RULES.REFRACTIVE_INDEX, theta1_deg=RULES.ANGLE_DEG` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Determine refract pathway angles across optical sheet discontinuities via Snell's framework. Project: Project Formulon-Physics |
| `wavedynamics` | `critical_angle(n1: float, n2: float)` | `n1=RULES.REFRACTIVE_INDEX, n2=RULES.REFRACTIVE_INDEX` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Isolate exact total internal reflection threshold angle points across interface boundaries ($\theta_c = \sin^{-1}(n_2 / n_1)$). Project: Project Formulon-Physics |
| `wavedynamics` | `mirror_focal_relation(u_obj: float, v_img: float)` | `u_obj=RULES.REAL, v_img=RULES.REAL` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Solve for reflective focal lengths matching coordinate object mapping surfaces ($ rac{1}{f} = rac{1}{v} + rac{1}{u}$). Project: Project Formulon-Physics |
| `wavedynamics` | `thin_lens_relation(u_obj: float, v_img: float)` | `u_obj=RULES.REAL, v_img=RULES.REAL` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Solve for single thin-refractive focal properties under paraxial approximations ($ rac{1}{f} = rac{1}{v} - rac{1}{u}$). Project: Project Formulon-Physics |
| `wavedynamics` | `lens_makers_equation(n_lens: float, n_medium: float, r1: float, r2: float)` | `n_lens=RULES.REFRACTIVE_INDEX, n_medium=RULES.REFRACTIVE_INDEX, r1=RULES.REAL, r2=RULES.REAL` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Evaluate structural focal parameters matching lens surface curvature indices via the Lens Maker formulation. $\frac{1}{f} = \left(\frac{n_{lens}}{n_{medium}} - 1\right)\left(\frac{ |
| `wavedynamics` | `lens_power(f_meters: float)` | `f_meters=RULES.REAL` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Calculate optical focal convergence capacity parameters in Diopters ($P = 1 / f$). Project: Project Formulon-Physics |
| `wavedynamics` | `mirror_magnification(u_obj: float, v_img: float)` | `u_obj=RULES.REAL, v_img=RULES.REAL` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Evaluate lateral magnification coefficients generated across mirror tracking spaces ($m = -v / u$). Project: Project Formulon-Physics |
| `wavedynamics` | `lens_magnification(u_obj: float, v_img: float)` | `u_obj=RULES.REAL, v_img=RULES.REAL` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Evaluate lateral magnification tracking dimensions matching refractive lens components ($m = v / u$). Project: Project Formulon-Physics |
| `wavedynamics` | `malus_law(i0: float, theta_deg: float)` | `i0=RULES.SOUND_INTENSITY, theta_deg=RULES.ANGLE_DEG` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Measure transmission flux reduction parameters crossing polarization planes via Malus's Law ($I = I_0 \cos^2\theta$). Project: Project Formulon-Physics |
| `wavedynamics` | `brewsters_angle(n1: float, n2: float)` | `n1=RULES.REFRACTIVE_INDEX, n2=RULES.REFRACTIVE_INDEX` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Isolate polarizing brewster reflection coordinate angle configurations ($ an heta_p = n_2 / n_1$). Project: Project Formulon-Physics |
| `wavedynamics` | `double_slit_fringe_width(wavelength: float, d_screen: float, d_slits: float)` | `wavelength=RULES.WAVELENGTH, d_screen=RULES.DISTANCE, d_slits=RULES.DISTANCE` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Calculate spatial interference fringe track separation width in Young's interference models ($\beta = \lambda D / d$). Project: Project Formulon-Physics |
| `wavedynamics` | `single_slit_diffraction_angle(slit_width_a: float, order_m: float, wavelength: float)` | `slit_width_a=RULES.DISTANCE, order_m=RULES.REAL, wavelength=RULES.WAVELENGTH` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Evaluate structural minimum diffraction dispersion vectors under single slit footprints ($a \sin\theta = m\lambda$). Project: Project Formulon-Physics |
| `wavedynamics` | `rayleigh_criterion(wavelength: float, aperture_diameter_d: float)` | `wavelength=RULES.WAVELENGTH, aperture_diameter_d=RULES.DISTANCE` | Acoustics, optics, vibration analysis, spectroscopy and wave propagation. Determine maximum angular resolution limits bounded under diffraction metrics ($\theta_{min} = 1.22 \lambda / D$). Project: Project Formulon-Physics |
